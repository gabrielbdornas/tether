# tether — default exclusions, applied to every captured path.
#
# rsync-style patterns. Add your own in ~/.config/tether/exclude.
# The credential entries below are the reason this file exists: a dotfile
# directory is a fine thing to sync between machines right up until it isn't.

# ---- caches and build output ----------------------------------------------
**/.git/
**/node_modules/
**/__pycache__/
**/.venv/
**/venv/
**/target/
**/dist/
**/build/
**/.cache/
**/Cache/
**/*.log
**/*.sock
**/*.pid

# ---- credentials: never leave the machine in the clear ---------------------
**/hosts.yml
**/credentials
**/credentials.json
**/.credentials.json
**/credentials.yml
**/*.key
**/*.pem
**/*_token
**/token.json
**/.netrc
**/known_hosts
**/.git-credentials
# A .env is a config file right up until it holds the key. `.config/git` itself
# stays: a gitconfig is worth carrying, a stored credential is not.
**/.env
**/.env.*

# systemd enable-state is captured declaratively, not as symlink farms
**/*.wants/

# ---- tether's own artifacts -------------------------------------------------
# Restore keeps replaced files as *.tether-bak. Capturing them back would
# accumulate a backup of a backup on every round trip.
**/*.tether-bak
# `ttr link` recreates this symlink; capturing it copies a frozen snapshot of
# the CLI that would then shadow the real one after a restore.
.local/bin/ttr
